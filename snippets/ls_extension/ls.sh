#
# Extends the `ls` command with addtional options for a better file listing by default.
#

# just define the alias
alias ls='ls -lFh'
# -l print in long list format
# -F appends a slash "/" to directories
# -h for "human readable" file sizes
